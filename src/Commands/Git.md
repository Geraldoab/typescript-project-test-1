## Git

### Creates a PR targeting the 'main' branch
```gh pr create --base main --title "feat: my title" --body "my body"```

### Merges the current branch's PR into main
```gh pr merge --squash --delete-branch```